{
  "version": 2,
  "buildCommand": "chmod +x node_modules/.bin/vite 2>/dev/null; npx vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
